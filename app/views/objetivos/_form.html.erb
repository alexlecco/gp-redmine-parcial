<%= simple_form_for(@objetivo) do |f| %>
  <% if @objetivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@objetivo.errors.count, "error") %> prohibited this objetivo from being saved:</h2>

      <ul>
      <% @objetivo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.input :lineamiento_id, collection: Lineamiento.all.collect { |p| [ p.nombre, p.id ] }, label: 'Lineamiento'%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
