<%= form_for(@lineamiento) do |f| %>
  <% if @lineamiento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lineamiento.errors.count, "error") %> prohibited this lineamiento from being saved:</h2>

      <ul>
      <% @lineamiento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
